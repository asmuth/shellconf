kde/keyboard:
  settings -> keyboard -> key bindings -> ctrl position -> "swap left win with left ctrl" -> on
  settings -> keyboard -> key bindings -> position of compose key -> "caps lock" -> on
  settings -> keyboard -> shortcuts -> unassign meta+a, meta+d, meta+p, meta+r, meta+t, meta+w, meta+left, meta+right
  settings -> keyboard -> shortcuts -> window management -> walk through windows -> meta+tab

kde/konsole:
  copy konsole/macos.keytab → ~/.local/share/konsole/macos.keytab
  copy konsole/Profile.profile → ~/.local/share/konsole/Profile.profile
  import keyboard shortcuts: konsole.shortcuts
